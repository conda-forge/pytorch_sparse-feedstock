{% set name = "torch-sparse" %}
{% set version = "0.6.12" %}


package:
  name: py{{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/torch_sparse-{{ version }}.tar.gz
  sha256: 85db85bd45855cde4be093c7e2413b962b21b31151ad7eacd19ca4e2808bced2

build:
  number: 0
  skip: true   # [win]
  script: 
    - export FORCE_CUDA="1"    # [linux64 and cuda_compiler_version not in (undefined, 'None')]
    - export CC="$GCC"   # [linux64 and cuda_compiler_version not in (undefined, 'None')]
    - {{ PYTHON }} -m pip install . -vv

requirements:
  build:
    - {{ compiler('c') }}

  host:
    - pip
    - python

  run:
    - python
    - pytorch >=1.8.0
    - scipy

test:
  imports:
    - torch_sparse
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/rusty1s/pytorch_sparse
  summary: PyTorch Extension Library of Optimized Autograd Sparse Matrix Operations
  license: MIT
  license_file: LICENSE
  dev_url: https://github.com/rusty1s/pytorch_sparse

extra:
  recipe-maintainers:
    - mxr-conda
    - rluria14
    - oblute
    - ndmaxar
